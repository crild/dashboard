# Local Worker secrets — never commit. See .dev.vars.example.
.dev.vars

# Wrangler local state (simulated KV lives here)
.wrangler/
node_modules/
*.log

# Personal financial config — belongs in Worker KV, never in this public repo.
spareplan-config.json
